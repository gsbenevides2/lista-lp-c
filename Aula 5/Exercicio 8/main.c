/*
Enunciado: Crie a função recursiva hanói(n,origem,auxiliar,destino), que resolve o problema das Torres de Hanói, movendo n discos da torre origem, para a torre destino, usando a torre auxiliar. Por exemplo, a chamada hanói(3,'A','B','C') deve resolver o problema discutido no Exemplo 7.
*/